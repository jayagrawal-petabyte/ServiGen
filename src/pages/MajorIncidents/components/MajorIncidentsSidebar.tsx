// Placeholder — full component coming soon
import React from 'react';
const MajorIncidentsSidebar: React.FC<{ onSelectList?: (id: string) => void }> = () => <div />;
export default MajorIncidentsSidebar;
