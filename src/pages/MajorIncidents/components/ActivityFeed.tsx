// Placeholder — full component coming soon
import React from 'react';
import type { ActivityFeedEntry } from '../types/majorIncident.types';
const ActivityFeed: React.FC<{ entries: ActivityFeedEntry[]; onClose: () => void }> = () => <div />;
export default ActivityFeed;
